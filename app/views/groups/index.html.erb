<h1>Groups</h1>
<% @groups.each do |group| %>
  <h3><%= link_to group.name, group %></h3>
  <p>Today <%= pluralize group.posts_for(Date.today).count, "post" %> -
     Yesterday <%= pluralize group.posts_for(Date.yesterday).count, "post" %></p>
<% end %>
